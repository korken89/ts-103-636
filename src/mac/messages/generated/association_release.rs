// GENERATED by codegen/src/defs/association_release.rs - DO NOT EDIT.
// Regenerate with `make codegen`.

//! AssociationReleaseParts generated codec.
//!
//! ETSI TS 103 636-4, clause 6.4.2.6, Figure 6.4.2.6-1, Table 6.4.2.6-1.
//!
//! Wire layout (ETSI bit numbering, column 0 = MSB):
//!
//! ```text
//!   0   1   2   3   4   5   6   7
//! +---------------+---------------+
//! | Release Cause |   Reserved    |
//! +---------------+---------------+
//! ```
//!
//! * Release Cause = cause

use crate::mac::pdu::MessageBody;
use crate::types::*;
use crate::{ParsingError, SerializationError};

/// Owned representation of an Association Release body (1 byte).
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[cfg_attr(feature = "defmt", derive(defmt::Format))]
pub struct AssociationReleaseParts {
    /// Release Cause (Table 6.4.2.6-1).
    pub cause: ReleaseCause,
}

impl AssociationReleaseParts {
    /// Body length in bytes (always 1).
    #[must_use]
    #[inline]
    pub const fn encoded_len(&self) -> usize {
        1
    }

    /// Serialize the body. Writes exactly 1 byte.
    ///
    /// # Errors
    ///
    /// Returns [`SerializationError::BufferTooShort`] if `out` is
    /// shorter than
    /// 1 byte.
    pub const fn serialize(&self, out: &mut [u8]) -> Result<usize, SerializationError> {
        if out.is_empty() {
            return Err(SerializationError::BufferTooShort);
        }
        out[0] = (self.cause.as_u8() & 0x0F) << 4;
        Ok(1)
    }

    /// Parse the bytes as `Self`.
    ///
    /// # Errors
    ///
    /// Returns [`ParsingError::Truncated`] on short input and
    /// [`ParsingError::ReservedValue`] when a field carries a
    /// reserved code point.
    pub const fn parse(buffer: &[u8]) -> Result<Self, ParsingError> {
        if buffer.is_empty() {
            return Err(ParsingError::Truncated);
        }
        let b0 = buffer[0];
        let Some(cause) = ReleaseCause::try_from_u8(b0 >> 4) else {
            return Err(ParsingError::ReservedValue);
        };
        Ok(Self { cause })
    }
}

impl MessageBody for AssociationReleaseParts {
    const IE_TYPE: IEType6bit = IEType6bit::AssociationRelease;
    #[inline]
    fn encoded_len(&self) -> usize {
        Self::encoded_len(self)
    }
    #[inline]
    fn serialize(&self, out: &mut [u8]) -> Result<usize, SerializationError> {
        Self::serialize(self, out)
    }
}
