// GENERATED by codegen/src/defs/measurement_report.rs - DO NOT EDIT.
// Regenerate with `make codegen`.

//! MeasurementReportParts generated codec.
//!
//! ETSI TS 103 636-4, clause 6.4.3.12, Figure 6.4.3.12-1, Table 6.4.3.12-1.
//!
//! Wire layout (ETSI bit numbering, column 0 = MSB):
//!
//! ```text
//!   0   1   2   3   4   5   6   7
//! +-----------+---+---+---+---+---+
//! | Reserved  |SNR|R2 |R1 |TX |RA |
//! +-----------+---+---+---+---+---+
//!
//! Present iff SNR is set:
//! +-------------------------------+
//! |              SNR              |
//! +-------------------------------+
//!
//! Present iff R2 is set:
//! +-------------------------------+
//! |            RSSI-2             |
//! +-------------------------------+
//!
//! Present iff R1 is set:
//! +-------------------------------+
//! |            RSSI-1             |
//! +-------------------------------+
//!
//! Present iff TX is set:
//! +-------------------------------+
//! |           TX Count            |
//! +-------------------------------+
//! ```
//!
//! * SNR = snr present
//! * R2 = rssi_2 present
//! * R1 = rssi_1 present
//! * TX = tx_count present
//! * RA = from_rach
//! * SNR = snr
//! * RSSI-2 = rssi_2
//! * RSSI-1 = rssi_1
//! * TX Count = tx_count

use crate::mac::pdu::MessageBody;
use crate::types::*;
use crate::{ExcessiveBitsSet, ParsingError};

/// Owned representation of a Measurement Report IE body.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[cfg_attr(feature = "defmt", derive(defmt::Format))]
pub struct MeasurementReportParts {
    /// RACH bit. `false` = measurement from scheduled resources; `true` = measurement from Random Access response.
    pub from_rach: bool,
    /// SNR measurement result.
    pub snr: Option<SnrMeasurement>,
    /// RSSI-2 measurement result.
    pub rssi_2: Option<Rssi2Measurement>,
    /// RSSI-1 measurement result.
    pub rssi_1: Option<Rssi1Measurement>,
    /// Number of transmitted packets since the last report.
    pub tx_count: Option<u8>,
}

impl MeasurementReportParts {
    /// Number of bytes [`Self::serialize`] will write.
    #[must_use]
    #[inline]
    pub const fn encoded_len(&self) -> usize {
        let mut len = 1;
        if self.snr.is_some() {
            len += 1;
        }
        if self.rssi_2.is_some() {
            len += 1;
        }
        if self.rssi_1.is_some() {
            len += 1;
        }
        if self.tx_count.is_some() {
            len += 1;
        }
        len
    }

    /// Serialize the body into `out`. Returns the number of bytes written.
    ///
    /// # Errors
    ///
    /// Returns [`ExcessiveBitsSet`] if the buffer is too short.
    pub const fn serialize(&self, out: &mut [u8]) -> Result<usize, ExcessiveBitsSet> {
        let len = self.encoded_len();
        if out.len() < len {
            return Err(ExcessiveBitsSet);
        }
        out[0] = (if self.from_rach { 0x01 } else { 0 })
            | (if self.snr.is_some() { 0x10 } else { 0 })
            | (if self.rssi_2.is_some() { 0x08 } else { 0 })
            | (if self.rssi_1.is_some() { 0x04 } else { 0 })
            | (if self.tx_count.is_some() { 0x02 } else { 0 });
        let mut pos = 1;
        if let Some(v) = self.snr {
            out[pos] = v.0;
            pos += 1;
        }
        if let Some(v) = self.rssi_2 {
            out[pos] = v.0;
            pos += 1;
        }
        if let Some(v) = self.rssi_1 {
            out[pos] = v.0;
            pos += 1;
        }
        if let Some(v) = self.tx_count {
            out[pos] = v;
            pos += 1;
        }
        Ok(pos)
    }

    /// Parse the bytes as `Self`.
    ///
    /// # Errors
    ///
    /// Returns [`ParsingError::Truncated`] on short input.
    pub const fn parse(buffer: &[u8]) -> Result<Self, ParsingError> {
        if buffer.is_empty() {
            return Err(ParsingError::Truncated);
        }
        let b0 = buffer[0];
        let snr_present = b0 & 0x10 != 0;
        let rssi_2_present = b0 & 0x08 != 0;
        let rssi_1_present = b0 & 0x04 != 0;
        let tx_count_present = b0 & 0x02 != 0;
        let from_rach = b0 & 0x01 != 0;
        let mut pos = 1;
        let snr = if snr_present {
            if buffer.len() < pos + 1 {
                return Err(ParsingError::Truncated);
            }
            let v = SnrMeasurement(buffer[pos]);
            pos += 1;
            Some(v)
        } else {
            None
        };
        let rssi_2 = if rssi_2_present {
            if buffer.len() < pos + 1 {
                return Err(ParsingError::Truncated);
            }
            let v = Rssi2Measurement(buffer[pos]);
            pos += 1;
            Some(v)
        } else {
            None
        };
        let rssi_1 = if rssi_1_present {
            if buffer.len() < pos + 1 {
                return Err(ParsingError::Truncated);
            }
            let v = Rssi1Measurement(buffer[pos]);
            pos += 1;
            Some(v)
        } else {
            None
        };
        let tx_count = if tx_count_present {
            if buffer.len() < pos + 1 {
                return Err(ParsingError::Truncated);
            }
            let v = buffer[pos];
            pos += 1;
            Some(v)
        } else {
            None
        };
        let _ = pos;
        Ok(Self {
            from_rach,
            snr,
            rssi_2,
            rssi_1,
            tx_count,
        })
    }
}

impl MessageBody for MeasurementReportParts {
    const IE_TYPE: IEType6bit = IEType6bit::MeasurementReport;
    #[inline]
    fn encoded_len(&self) -> usize {
        Self::encoded_len(self)
    }
    #[inline]
    fn serialize(&self, out: &mut [u8]) -> Result<usize, ExcessiveBitsSet> {
        Self::serialize(self, out)
    }
}
